# pnpm 11+: her komut öncesi deps status check'i kapat —
# esbuild build approve uyarısı her run'da exit 1 veriyordu.
verify-deps-before-run=false

# Workspace içi peer dep katı doğrulamayı sustur
strict-peer-dependencies=false
auto-install-peers=true
